name: md_link_auditor
description: A CLI tool to find broken markdown links.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  http: ^1.1.0
  glob: ^2.1.2
  collection: ^1.17.2

dev_dependencies:
  test: ^1.24.0